import java.io.IOException;
///                                 JAVA throw and throws..🎯🎯
 
/// -- We use throw keyword in the (method declaration) to (declare the type of exceptions) that might occur within it.
/// -- method ko declare kerte hai (like is type ka exception aa sakta hai)
/// 
/// 
public class Throwss {
    public static void findFile() throws IOException{
        // code that may produce IOException
        File newFile = new File();
    }
}
